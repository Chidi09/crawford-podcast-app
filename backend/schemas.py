    # backend/schemas.py

    # This file now serves as a placeholder or could be used for re-exporting
    # common schemas if desired. All specific schemas have been moved to
    # their respective modular files within the 'backend/schemas' package.

    # UserBase, UserUpdate, UserResponse are in backend.schemas.user
    # UserCreate, Token, TokenData are in backend.schemas.auth
    # Podcast schemas are in backend.schemas.podcast
    # Live Stream schemas are in backend.schemas.live_stream
    